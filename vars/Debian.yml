---

default_pdns_rec_user: "pdns"
default_pdns_rec_group: "pdns"
default_pdns_rec_config_dir: "/etc/powerdns"

# The name of the debug package
default_pdn_recorsor_debug_symbols_package_name: "pdns-recursor-dbg"

